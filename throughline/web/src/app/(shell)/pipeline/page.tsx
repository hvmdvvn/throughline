import { PlaceholderPage } from "@/components/placeholder-page";

export default function PipelinePage() {
  return (
    <PlaceholderPage
      title="Pipeline"
      description="Placeholder for the Phase 1 requirement pipeline board (states, not tasks)."
    />
  );
}
